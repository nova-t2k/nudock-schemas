{
  "$id": "get_parameters",
  "type": "object",
  "properties": {
    "request": {
      "type": "string",
      "additionalProperties": false
    },
    "response": {
      "type": "object",
      "properties": {
        "sys_pars": {
          "type": "object",
          "description": "Mapping of systematic parameter names to their current numerical values",
          "additionalProperties": { "type": "number" }
        },
        "osc_pars": {
          "type": "object",
          "description": "Mapping of oscillation parameter names to their current numerical values, in NuDock convention (e.g. Theta23 rather than sin2th_23). Includes any extended-model parameters such as NSI.",
          "additionalProperties": { "type": "number" }
        },
        "duration_us": { "type": "number" }
      },
      "required": ["sys_pars", "osc_pars"],
      "additionalProperties": false
    }
  },
  "required": ["request", "response"],
  "additionalProperties": false
}
